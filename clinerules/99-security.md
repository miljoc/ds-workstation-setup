# Security

Never expose:

Passwords

JWT secrets

API keys

Database credentials

Never log secrets.

Always validate external input.

Prefer parameterized queries.

Escape HTML where appropriate.

Assume all user input is untrusted.

Never disable authentication unless explicitly requested.

Follow OWASP best practices.
